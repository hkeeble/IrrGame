/**
 * @file 	ChunkManager.h
 * @author 	Henri Keeble
 * @brief 	Declares a class used to manage a set of chunks.
 * @see 	ChunkManager
 */

/* 
#ifndef CHUNK_MANAGER_H
#define CHUNK_MANAGER_H

#include "Chunk.h"

namespace IrrGame
{
	class ChunkManager
	{
	public:
		ChunkManager();
		ChunkManager(const u32& width, const u32& height, const u32& depth);
		ChunkManager(const ChunkManager& param);
		const ChunkManager& operator=(const ChunkManager& param);
		Chunk* operator() 
		virtual ~ChunkManager();
		
	private:
		u32 width, height, depth;
	};
}

#endif // CHUNK_MANAGER_H
*/
